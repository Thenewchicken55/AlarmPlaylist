# YouTube Data API (optional — for importing YouTube playlists)
# Get credentials: https://console.cloud.google.com/apis/credentials
VITE_YOUTUBE_CLIENT_ID=your-youtube-client-id.apps.googleusercontent.com

# Spotify Web API (optional — for importing Spotify playlists)
# Get credentials: https://developer.spotify.com/dashboard
VITE_SPOTIFY_CLIENT_ID=your-spotify-client-id

# Spotify redirect URI (must match the URI registered in Spotify Dashboard)
# Defaults to window.location.origin + BASE_URL + "spotify-callback"
# Override if your deployed app uses a different callback path
# VITE_SPOTIFY_REDIRECT_URI=https://your-domain.com/AlarmPlaylist/spotify-callback
